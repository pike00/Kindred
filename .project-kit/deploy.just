# .project-kit/deploy.just — generated for kindred (target=homelab)
# Generated by project-kit (skill version: 0.2.0)
# Last refreshed: 2026-09-21

deploy TAG="" ENV="prod":
    @just context deploy
    @uv run .project-kit/scripts/deploy-homelab.py --env {{ENV}} --tag "{{TAG}}"
deploy-dry TAG="" ENV="prod":
    @just context deploy
    @uv run .project-kit/scripts/deploy-homelab.py --env {{ENV}} --tag "{{TAG}}" --dry-run

rollback ENV="prod":
    @just context deploy
    @uv run .project-kit/scripts/deploy-homelab.py --env {{ENV}} --rollback

status ENV="prod":
    @just context deploy
    @uv run .project-kit/scripts/deploy-homelab.py --env {{ENV}} --status
