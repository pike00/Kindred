# .project-kit/db.just — generated for kindred (backend=none)
# Generated by project-kit (skill version: 0.2.0)
# Last refreshed: 2026-09-21

db-migrate:
    @just _skip "db-migrate"

db-rollback:
    @just _skip "db-rollback"

db-fresh:
    @just _skip "db-fresh"

db-shell:
    @just _skip "db-shell"

db-dump:
    @just _skip "db-dump"

db-restore FILE:
    @just _skip "db-restore"

db-seed:
    @just _skip "db-seed"
