meta {
  name: List Interactions
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/interactions/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

params:query {
  skip: 0
  limit: 20
}
