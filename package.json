{
  "name": "personal-crm",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter frontend dev",
    "lint": "bun run --filter frontend lint",
    "e2e": "for f in e2e/*.test.ts; do bun run \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
